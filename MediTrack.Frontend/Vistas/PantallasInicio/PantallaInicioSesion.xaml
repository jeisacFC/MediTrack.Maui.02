<?xml version="1.0" encoding="utf-8" ?>
<base:BaseContentPage
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewModels="clr-namespace:MediTrack.Frontend.ViewModels.PantallasInicio"
    xmlns:base="clr-namespace:MediTrack.Frontend.Vistas.Base"
    x:Class="MediTrack.Frontend.Vistas.PantallasInicio.PantallaInicioSesion"
    NavigationPage.HasNavigationBar="False">

    <Grid>

        <!-- Fondo con patrón, siempre al fondo -->
        <Image Source="logo_patron.png"
           Aspect="AspectFill"
           Opacity="0.8"
           HorizontalOptions="Fill"
           VerticalOptions="Fill"
           ZIndex="0" />

        <!-- Layout principal dividido en dos filas -->
        <Grid RowDefinitions="*,3*"
          ZIndex="1">

            <!-- Fila 0: espacio para el fondo -->
            <Grid Grid.Row="0" />

            <!-- Fila 1: contenedor blanco con formulario -->
            <Frame Grid.Row="1"
             CornerRadius="40"
             BackgroundColor="White"
             HasShadow="True"
             Margin="0"
             Padding="35"
             VerticalOptions="Fill"
             HeightRequest="800">

                <ScrollView>
                    <VerticalStackLayout Spacing="25">

                        <!-- Logo pequeño -->
                        <Image Source="logo_meditrack.png"
                   HeightRequest="100"
                   HorizontalOptions="Center"
                   Margin="0,10"/>

                        <!-- Título -->
                        <Label Text="Iniciar Sesión"
                   FontSize="28"
                   FontAttributes="Bold"
                   TextColor="#3B71FF"
                   HorizontalOptions="Center"/>

                        <!-- Mensaje de estado -->
                        <Label Text="{Binding MensajeEstado}"
                               TextColor="#3B71FF"
                               FontSize="14"
                               HorizontalOptions="Center"
                               IsVisible="{Binding MensajeEstado, Converter={StaticResource StringNotEmptyConverter}}"/>

                        <!-- Correo electrónico -->
                        <Frame CornerRadius="12"
                   BackgroundColor="#F8F8F8"
                   HasShadow="False"
                   Padding="10">
                            <HorizontalStackLayout>
                                <Image>
                                    <Image.Source>
                                        <FontImageSource Glyph="&#xE0BE;"
                                     FontFamily="MaterialIcons"
                                     Color="#3B71FF"
                                     Size="20"/>
                                    </Image.Source>
                                </Image>
                                <Entry x:Name="EmailEntry"
                                       Text="{Binding Email}"
                                       Placeholder="Correo electrónico"
                                       Keyboard="Email"
                                       PlaceholderColor="#A0A0A0"
                                       TextColor="Black"
                                       IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"
                                       Margin="10,0"/>
                            </HorizontalStackLayout>
                        </Frame>

                        <!-- Contraseña -->
                        <Frame CornerRadius="12"
                   BackgroundColor="#F8F8F8"
                   HasShadow="False"
                   Padding="10">
                            <HorizontalStackLayout>
                                <Image>
                                    <Image.Source>
                                        <FontImageSource Glyph="&#xE897;"
                                     FontFamily="MaterialIcons"
                                     Color="#3B71FF"
                                     Size="20"/>
                                    </Image.Source>
                                </Image>
                                <Entry x:Name="ContraseñaEntry"
                                       Text="{Binding Contraseña}"
                                       Placeholder="Contraseña"
                                       IsPassword="True"
                                       PlaceholderColor="#A0A0A0"
                                       TextColor="Black"
                                       IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"
                                       Margin="10,0"/>
                            </HorizontalStackLayout>
                        </Frame>

                        <!-- Recordar sesión -->
                        <HorizontalStackLayout HorizontalOptions="Start" Spacing="10">
                            <CheckBox IsChecked="{Binding RecordarSesion}"
                                      Color="#3B71FF"
                                      IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"/>
                            <Label Text="Recordar sesión"
                                   TextColor="Gray"
                                   VerticalOptions="Center"/>
                        </HorizontalStackLayout>

                        <!-- Botón Ingresar -->
                        <Button Text="Ingresar"
                                Command="{Binding LoginCommand}"
                                FontSize="18"
                                BackgroundColor="#3B71FF"
                                TextColor="White"
                                CornerRadius="12"
                                HeightRequest="50"
                                IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}"
                                Clicked="IniciarSesion">
                            <Button.Triggers>
                                <DataTrigger TargetType="Button" Binding="{Binding IsLoading}" Value="True">
                                    <Setter Property="Text" Value="Cargando..." />
                                </DataTrigger>
                            </Button.Triggers>
                        </Button>

                        <!-- ¿Olvidaste tu contraseña? -->
                        <Label Text="¿Olvidaste tu contraseña?"
                               TextColor="#3B71FF"
                               FontAttributes="Italic"
                               HorizontalOptions="Center">
                            <Label.GestureRecognizers>
                                <TapGestureRecognizer Command="{Binding IrAOlvidoContraseñaCommand}" 
                                                      Tapped="IrAOlvidoContrasena" />
                            </Label.GestureRecognizers>
                        </Label>

                        <!-- Divider -->
                        <BoxView HeightRequest="1"
                     BackgroundColor="#E0E0E0"/>

                        <!-- Registro: ahora usando Grid para alinear -->
                        <Grid Margin="60,0,0,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!-- Texto a la izquierda -->
                            <Label Text="¿No tienes cuenta?"
                     TextColor="Gray"
                     VerticalOptions="Center"
                     Grid.Column="0" />

                            <!-- Botón a la derecha -->
                            <Button Text="Regístrate"
                                    Command="{Binding IrARegistroCommand}"
                                    BackgroundColor="Transparent"
                                    TextColor="#3B71FF"
                                    FontAttributes="Bold"
                                    VerticalOptions="Center"
                                    Grid.Column="1"
                                    Clicked="IrARegistro"/>
                        </Grid>

                    </VerticalStackLayout>
                </ScrollView>

            </Frame>
        </Grid>
    </Grid>
</base:BaseContentPage>