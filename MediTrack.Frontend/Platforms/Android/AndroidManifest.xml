<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android" package="com.companyname.meditrack.frontend">
	<application android:allowBackup="true" android:supportsRtl="true" android:icon="@mipmap/logo_meditrack_appicon"></application>
	
	<!-- Permisos necesarios -->
	
	<!-- Permisos para cámara y escaneo -->
	<uses-permission android:name="android.permission.CAMERA" />

	<!-- Características de hardware (optional=false para que funcione en emuladores) -->
	<uses-feature android:name="android.hardware.camera" android:required="false" />
	<uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
	<uses-feature android:name="android.hardware.camera.flash" android:required="false" />

	<!-- Permiso adicional recomendado para mejor rendimiento -->
	<uses-permission android:name="android.permission.VIBRATE" />


</manifest>